{% extends 'base.html.twig' %}

{% block title %}Matiere{% endblock %}

{% block body %}
<div class="container">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Matière</h4>
            <p class="card-text">
            <table class="table">
                <tbody>
            <tr>
                <th>Id</th>
                <td>{{ matiere.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ matiere.titre }}</td>
            </tr>
            <tr>
                <th>Reference</th>
                <td>{{ matiere.reference }}</td>
            </tr>
        </tbody>
            </table>    
            <center>
            <a class="btn btn-primary" href="{{ path('matiere_edit', {'id': matiere.id}) }}">Modifier </a> <br>

            {{ include('matiere/_delete_form.html.twig') }}<br>

            <a href="{{ path('matiere_index') }}">Retour à la liste</a>


        </center>

            </p>
        </div>
    </div>
</div>
{% endblock %}
