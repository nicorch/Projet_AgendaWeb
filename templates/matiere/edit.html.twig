{% extends 'base.html.twig' %}

{% block title %}Edit Matiere{% endblock %}

{% block body %}
    <div class="container">
    <br>
        <div class="card">
            <div class="card-body">
                <h4 class="card-title">Edit une Matiere</h4>
                <p class="card-text">
                    {{ include('matiere/_form.html.twig', {'button_label': 'Modifier'}) }}<br>
                    <center>{{ include('matiere/_delete_form.html.twig') }}</center>
                    <a href="{{ path('matiere_index') }}">Retour à la liste</a>
                </p>
            </div>
        </div>
    </div>
{% endblock %}
