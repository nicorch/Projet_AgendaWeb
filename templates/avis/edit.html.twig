{% extends 'base.html.twig' %}

{% block title %}Edit Avis{% endblock %}

{% block body %}
    <div class="container">
    <br>
        <div class="card">
            <div class="card-body">
                <h4 class="card-title">Edit un Avis</h4>
                <p class="card-text">
                    {{ include('avis/_form.html.twig', {'button_label': 'Modifier'}) }}
                    {{ include('avis/_delete_form.html.twig') }}
                    <a href="{{ path('avis_index') }}">Retour à la liste</a>
                </p>
            </div>
        </div>
    </div>

    


{% endblock %}
